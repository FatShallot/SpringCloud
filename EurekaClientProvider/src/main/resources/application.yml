server:
  port: 21000
spring:
  application:
    # 服务注册在注册中心上的名字
    name: provider
eureka:
  client:
    service-url:
      # 注册中心的访问地址
      defaultZone: http://localhost:8761/eureka/
  instance:
    # 是否将当前服务的ip注册到注册中心上
   prefer-ip-address: true