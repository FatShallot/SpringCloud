# 注册中心端口常用为8761
server:
  port: 8761
eureka:
  client:
    # 是否作为客户端注册到注册中心上，它自己就是注册中心所以不用注册
    register-with-eureka: false
    # 是否获取其他注册中心的数据
    fetch-registry: false
    # 注册中心的访问地址
    service-url:
      defaultZone: http://localhost:8761/eureka/